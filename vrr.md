# VRR на Steam Deck: Техническое расследование

**Статус:** ❌ Невозможно (LCD и OLED)  
**Причина:** LCD — мост ANX7530, OLED — заблокировано в firmware  
**Дата:** Январь 2025

---

## TL;DR

VRR (Variable Refresh Rate / FreeSync) на Steam Deck **не работает ни на LCD, ни на OLED**:
- **LCD:** Аппаратное ограничение моста Analogix ANX7530 (MIPI DSI 1.2 не поддерживает динамический VBLANK)
- **OLED:** Панель Samsung поддерживает VRR, но Valve заблокировала функцию в firmware/драйвере

---

## 1. Аппаратная архитектура

### Цепочка видеосигнала

```
Steam Deck LCD:
┌─────────────┐    DisplayPort 1.4    ┌─────────────┐    MIPI DSI 1.2    ┌─────────────┐
│  APU        │ ──────────────────► │  ANX7530    │ ──────────────────► │  Панель     │
│  Van Gogh   │                      │  (Bridge)   │                      │  BOE/JDI    │
└─────────────┘                      └─────────────┘                      └─────────────┘
                                           ▲
                                           │
                                      УЗКОЕ МЕСТО
```

```
Steam Deck OLED:
┌─────────────┐         eDP 1.4           ┌─────────────┐
│  APU        │ ────────────────────────► │  Samsung    │  ❌ VRR заблокирован
│  Sephiroth  │                           │  AMOLED     │     в firmware
└─────────────┘                           └─────────────┘
```

### Проблема: Analogix ANX7530

| Параметр | Вход | Выход |
|----------|------|-------|
| Протокол | DisplayPort 1.4 | MIPI DSI 1.2 |
| VRR/Adaptive Sync | ✅ Поддерживается | ❌ Не поддерживается |
| Динамический VBLANK | ✅ Да | ❌ Нет |

**Спецификация MIPI DSI 1.2 (Video Mode)** не поддерживает динамическое изменение интервала вертикального гашения (VBLANK) — это основа работы VRR.

---

## 2. Почему программные решения не работают

### Попытка 1: Патч ядра amdgpu

**Идея:** Принудительно сообщить драйверу, что экран поддерживает FreeSync (игнорируя EDID).

**Результат:**
```
APU отправляет VRR-сигнал
        ↓
ANX7530 получает нестандартные тайминги
        ↓
Потеря синхронизации с панелью
        ↓
Link Retraining (полный сброс соединения)
        ↓
💀 Чёрный экран 1-2 секунды
```

### Попытка 2: Эмуляция плагинами (SmartRefresh)

**Идея:** Демон в userspace следит за FPS и меняет герцовку через Mode Set.

**Почему это не VRR:**

| Проблема | Описание |
|----------|----------|
| **Atomic Mode Setting** | Смена частоты — это смена видеорежима, а не покадровая подстройка. Требует перенастройки PLL (Pixel Clock). |
| **Задержка** | Мониторинг → Решение → Применение. К моменту переключения сцена уже изменилась. |
| **Чёрный экран** | На LCD смена режима вызывает гашение на ~1 секунду. |

**Вердикт:** Неиграбельно в динамичных играх.

---

## 3. Сравнение устройств

| Устройство | Архитектура экрана | VRR | Причина |
|------------|-------------------|-----|---------|
| **Steam Deck LCD** | APU → ANX7530 → Панель | ❌ Невозможно | Мост не транслирует VRR в MIPI DSI |
| **Steam Deck OLED** | APU → Панель | ❌ Невозможно | Та же проблема — нет поддержки в firmware |
| **Nintendo Switch** | Tegra → Панель (напрямую) | ⚠️ Эмуляция | Прямой доступ к регистрам Tegra DC |

### Steam Deck OLED — тоже без VRR!

**Распространённый миф:** "На OLED есть VRR, потому что нет моста ANX7530".

**Реальность:** VRR на Steam Deck OLED **тоже не работает**. 

#### Архитектура OLED

```
Steam Deck OLED:
┌─────────────┐         eDP 1.4          ┌─────────────┐
│  APU        │ ────────────────────────► │  Samsung    │
│  Sephiroth  │                           │  AMOLED     │
│  (6nm)      │                           │  90Hz       │
└─────────────┘                           └─────────────┘
                                                │
                                          Панель МОЖЕТ VRR
                                          Но firmware НЕ ДАЁТ
```

#### Почему не работает

| Компонент | Поддержка VRR | Статус |
|-----------|---------------|--------|
| Панель Samsung AMOLED | ✅ Да (аппаратно) | Способна |
| APU Sephiroth (eDP 1.4) | ✅ Да | Способен |
| Драйвер amdgpu | ⚠️ Частично | Код есть, но... |
| **Firmware Valve** | ❌ **Заблокировано** | Не активировано |

#### Техническое объяснение

1. **Панель поддерживает VRR:** Samsung AMOLED в Steam Deck OLED физически способна работать в диапазоне ~48-90Hz с переменной частотой.

2. **eDP 1.4 поддерживает Adaptive Sync:** В отличие от LCD (где мост ANX7530 конвертирует в MIPI DSI), OLED подключен напрямую через eDP, который поддерживает VRR.

3. **Valve не активировала функцию:** В firmware/драйвере VRR для встроенного экрана **отключен**. EDID панели не сообщает о поддержке Adaptive Sync (хотя панель умеет).

#### Доказательства

```bash
# Проверка VRR capability на OLED
cat /sys/class/drm/card0-eDP-1/vrr_capable
# Результат: 0 (не поддерживается)

# На внешнем мониторе с FreeSync:
cat /sys/class/drm/card0-DP-1/vrr_capable  
# Результат: 1 (поддерживается)
```

#### Почему Valve не включила VRR на OLED

| Причина | Объяснение |
|---------|------------|
| **Калибровка цвета** | VRR может влиять на цветопередачу OLED при разных частотах |
| **Burn-in риски** | Переменная частота может неравномерно нагружать пиксели |
| **Стабильность** | "It just works" — Valve предпочитает предсказуемое поведение |
| **Батарея** | VRR может увеличить потребление в некоторых сценариях |

#### Можно ли разблокировать?

**Теоретически:** Да, через модификацию EDID или патч драйвера.

**Практически:** 
- Требует модификации firmware панели или драйвера
- Valve может сломать при обновлении SteamOS
- Риск повреждения панели (burn-in, мерцание)
- Никто пока не сделал рабочий патч

### Почему на Switch работает "эмуляция"

Switch использует ARM-архитектуру с прямым доступом к регистрам Display Controller (Tegra DC). Это позволяет менять клоки **в реальном времени** без Link Retraining.

На Steam Deck (обе версии) между пользователем и железом стоит стек абстракций (ACPI, драйвер, firmware), который не даёт прямого контроля.

---

## 4. Технические детали

### Даташит ANX7530

```
Входной интерфейс:  DisplayPort 1.4 (HBR3, 8.1 Gbps/lane)
Выходной интерфейс: MIPI DSI 1.2 (4 lanes, Video Mode)
Управление:         I2C (0x70, 0x72)
VRR Support:        НЕТ (не указан в спецификации)
```

### Что происходит при смене частоты

```c
// Псевдокод поведения ANX7530
void on_timing_change(timing_t new_timing) {
    if (new_timing != current_timing) {
        // Мост не умеет менять тайминги на лету
        trigger_link_retraining();  // ~1-2 секунды чёрного экрана
        reinitialize_dsi_link();
        apply_new_timing(new_timing);
    }
}
```

---

## 5. Итоговый вердикт

### ❌ VRR на Steam Deck невозможен (обе версии)

| Версия | Причина | Тип ограничения |
|--------|---------|-----------------|
| **LCD** | Мост ANX7530 + MIPI DSI 1.2 | Аппаратное (железо) |
| **OLED** | Valve заблокировала в firmware | Программное (но не обойти) |

### LCD — проблема железа

Мост Analogix ANX7530 физически не умеет транслировать VRR-сигнал в протокол MIPI DSI 1.2. Это нельзя исправить софтом.

### OLED — проблема политики Valve

Панель Samsung AMOLED **способна** на VRR, но Valve решила не активировать эту функцию. Причины: стабильность, калибровка цвета, риски burn-in.

### Что делать пользователям

| Решение | Эффект |
|---------|--------|
| **40Hz режим** | Стабильный фреймрейт, экономия батареи |
| **Frame limiter** | Убирает tearing при фиксированной частоте |
| **Внешний монитор с FreeSync** | Единственный способ получить VRR на Steam Deck |

---

## Ссылки

- [Analogix ANX7530 Product Brief](https://www.analogix.com)
- [MIPI DSI 1.2 Specification](https://www.mipi.org)
- [amdgpu VRR implementation](https://github.com/torvalds/linux/blob/master/drivers/gpu/drm/amd/display/)

---

*Исследование завершено: Январь 2026*
